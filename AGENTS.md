# AGENTS.md

## Commands

```bash
# Run dev server (when implemented)
npm run dev

# Run tests (when implemented)
npm test

# Lint and typecheck (when implemented)
npm run lint
npm run typecheck
```

## Notes

- This directory is currently empty. When adding code:
  - Create `package.json` with scripts for `dev`, `test`, `lint`, `typecheck`
  - Add a `README.md` with setup and usage instructions
  - Configure lint/typecheck tools following language/framework best practices
- Verify all commands work before committing